import { withInstall } from '@nopon-web/utils/vue';
import {{PascalName}}BasicInstace from './{{name}}-basic.vue';

export const {{PascalName}}Basic = withInstall({
  ...{{PascalName}}BasicInstace,
  name: '{{PascalName}}Basic',
});

export default [{{PascalName}}Basic];
