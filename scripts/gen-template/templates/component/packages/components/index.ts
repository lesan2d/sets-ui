import { withInstall } from '@nopon-web/utils/vue';
import {{PascalName}} from './src/{{name}}.vue';

export const S{{PascalName}} = withInstall({{PascalName}} as any);

export default S{{PascalName}};
